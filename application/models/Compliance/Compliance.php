<?php

class Compliance extends CI_Controller {

    public $field_keys;

    function __construct() {
        parent::__construct();
    }

    function index() {
        
    }

    function page($pageName = "", $data = array(), $scripts = array(), $stylesheets = array()) {
        
    }

    function form($formName = "", $data = array(), $scripts = array(), $stylesheets = array()) {
        
    }

    function modal($modalName = "", $data = array(), $scripts = array(), $stylesheets = array()) {
        
    }

    function ajax($fileName = "", $data = array(), $scripts = array(), $stylesheets = array()) {
        
    }

}
